#!/usr/bin/env bash

for file in "$@"
do
  basefile="${file%.*}"
  out1="$basefile.signed.pdf"
  out2="$basefile.signed.signed.pdf"

  pdf-sign --no-flatten "$file"
  pdf-sign --no-flatten "$out1"
  mv "$out2" "$out1"
done
