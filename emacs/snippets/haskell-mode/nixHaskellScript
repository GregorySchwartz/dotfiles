#name : NixHaskellScript
# --
#! /usr/bin/env nix-shell
#! nix-shell -E "let config = {allowBroken = true;}; in with import <nixpkgs> { inherit config; }; runCommand \"dummy\" { buildInputs = [ (hp.ghcWithPackages (pkgs: [pkgs.system-filepath pkgs.text pkgs.turtle pkgs.bytestring pkgs.containers]))]; } \"\""
#! nix-shell -i runghc

{-# LANGUAGE OverloadedStrings #-}

import qualified Data.Text as T
import qualified Filesystem.Path as FP
import qualified Turtle as TU
import qualified Turtle.Bytes as TUB
import qualified Data.ByteString.Char as B

main :: IO ()
main = sh $ do
  $1